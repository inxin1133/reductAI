# ai-agent-service 환경변수 예시
# 이 파일을 .env로 복사한 뒤 필요한 값으로 수정하세요.
#
# 채팅 및 크레딧 차감에 필수:
CREDITS_SERVICE_URL=http://localhost:3011
CREDITS_SERVICE_KEY=reductai-credits-internal
#
# Docker 사용 시: docker-compose가 auth-service/.env를 env_file로 로드합니다.
# 로컬에서 개별 실행 시: 이 디렉토리에 .env를 만들어 위 값을 설정하세요.
